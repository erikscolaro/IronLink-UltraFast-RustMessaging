# ============================================================
# DATABASE CONFIGURATION
# ============================================================
# Per l'applicazione: usa l'utente dedicato 'ruggine'
DATABASE_URL=mysql://ruggine:ferro@127.0.0.1:3306/rugginedb

# Per i test: SQLx richiede un utente con privilegi di superuser (root)
# per creare/distruggere database di test automaticamente.
# Durante i test, #[sqlx::test] usa DATABASE_URL (questa variabile)
# per creare database temporanei isolati (es: _sqlx_test_1234)
# IMPORTANTE: Assicurati che l'utente qui specificato abbia privilegi di CREATE DATABASE
# 
# Per configurare l'utente di test con root:
# 1. Cambia temporaneamente DATABASE_URL prima di eseguire `cargo test`:
#    DATABASE_URL=mysql://root:your_root_password@127.0.0.1:3306
# 2. Oppure crea un .env.test con DATABASE_URL=mysql://root:...
#
# In produzione: usa solo l'utente 'ruggine' con privilegi limitati

# JWT Secret (REQUIRED for production!)
JWT_SECRET=ilmiobellissimosegretochevaassolutamentecambiato

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Database Pool Configuration
MAX_DB_CONNECTIONS=1000
DB_CONNECTION_LIFETIME_SECS=1

# Application Environment
# Values: development, production, test
APP_ENV=development

# Logging Configuration
# Values: trace, debug, info, warn, error
LOG_LEVEL=info